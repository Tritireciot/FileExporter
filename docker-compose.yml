services:
  backend:
    container_name: backend
    build:
      context: ./
      dockerfile: Dockerfile
    restart: always
    env_file: ".env"
    ports:
      - 8153:8153
    networks:
      - app


networks:
  app: